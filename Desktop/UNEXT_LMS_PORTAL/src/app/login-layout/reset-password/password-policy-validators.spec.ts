import { PasswordPolicyValidators } from './password-policy-validators';

describe('PasswordPolicyValidators', () => {
  it('should create an instance', () => {
    expect(new PasswordPolicyValidators()).toBeTruthy();
  });
});
